# Dashboard settings

## Approved nodes

**None.** No Figma frame named settings/profile-password/account.

Sidebar items mention Settings (`102:1673`, DS `102:6323`) but there is no page artboard.

## Route / code

`/[lang]/specialist/dashboard/settings` → `ChangePasswordForm`. Headings currently not fully i18n.

## Implementation

Do not invent a Figma-driven redesign. If styling, reuse the **chosen** dashboard shell + `components/ui` only.

## Risk

No visual target. Leave until a frame exists or a human specifies Language A/B chrome only.
